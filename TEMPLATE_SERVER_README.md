# PPTist 模板服务器使用说明

## 功能概述

本项目在原 PPTist 基础上增加了**服务器端模板存储**功能，支持：

- ✅ **跨设备共享默认模板**：在任何设备访问都能看到相同的默认模板
- ✅ **密码保护**：设置、修改、清除模板都需要密码验证
- ✅ **自动适配网络**：支持 localhost、IP 地址等多种访问方式
- ✅ **密码管理**：支持修改密码功能

## 快速开始

### 1. 启动后端服务器（端口 3000）

```bash
cd server
npm install  # 首次运行需要安装依赖
npm start
```

服务器将在以下地址运行：
- 本地访问：http://localhost:3000
- 局域网访问：http://0.0.0.0:3000

### 2. 启动前端开发服务器（端口 5173）

在项目根目录：

```bash
npm install  # 首次运行需要安装依赖
npm run dev
```

前端将在以下地址运行：
- http://localhost:5173
- http://127.0.0.1:5173
- http://[本机IP]:5173

## 功能使用说明

### 首次设置默认模板

1. 上传 PPTX 文件或手动编辑 PPT 内容
2. 点击左上角菜单 → **"设为默认模板"**
3. 在弹出的对话框中输入密码（首次设置时，这个密码将成为管理密码）
4. 点击"确定"
5. 看到"已设为默认模板"提示

**注意**：首次设置的密码非常重要，请妥善保管！

### 在其他设备使用

1. 在其他电脑/手机的浏览器访问：`http://[服务器IP]:5173`
2. 页面会自动加载服务器上保存的默认模板
3. 无需任何操作，模板自动同步

### 修改默认模板

1. 编辑 PPT 内容
2. 点击菜单 → **"设为默认模板"**
3. 输入之前设置的密码
4. 点击"确定"
5. 所有设备刷新后都会看到新模板

### 清除默认模板

1. 点击菜单 → **"清除默认模板"**
2. 输入密码
3. 点击"确定"
4. 模板被清除，恢复到系统默认状态

### 修改密码

1. 点击菜单 → **"修改密码"**
2. 在对话框中：
   - 第一个输入框：输入旧密码
   - 第二个输入框：输入新密码
3. 点击"确定"
4. 看到"密码修改成功"提示

## 安全说明

- 🔒 **密码加密存储**：密码使用 SHA-256 加密，不会明文保存
- 🔒 **操作验证**：所有模板修改操作都需要密码验证
- 🔒 **权限控制**：只有知道密码的人才能修改或清除默认模板
- 🔒 **密码修改**：支持修改密码，需要验证旧密码

## 技术架构

### 后端（Express + Node.js）
- **端口**：3000
- **API 端点**：
  - `GET /api/template` - 获取默认模板
  - `POST /api/template` - 保存默认模板（需要密码）
  - `DELETE /api/template` - 清除默认模板（需要密码）
  - `POST /api/change-password` - 修改密码（需要旧密码）

### 前端（Vue 3 + Vite）
- **端口**：5173
- **技术栈**：Vue 3 Composition API + Pinia + TypeScript
- **自动适配**：API 地址根据访问主机名自动调整

### 数据存储
- **位置**：`server/data/` 目录
- **文件**：
  - `default-template.json` - 模板数据（slides + theme）
  - `password.json` - 密码哈希值

## 注意事项

### 1. 服务器必须保持运行
前端和后端服务器都需要同时运行，模板功能才能正常工作。

### 2. 局域网访问
确保防火墙允许以下端口：
- **3000**：后端 API 服务
- **5173**：前端页面服务

### 3. 数据备份
重要模板建议定期备份 `server/data` 目录。

### 4. 密码找回
如果忘记密码，可以删除 `server/data/password.json` 文件，下次设置模板时会创建新密码。

**警告**：删除密码文件后，需要重新设置模板。

## 故障排除

### 问题：手机访问看不到模板

**解决方案**：
1. 确保手机和电脑在同一局域网
2. 检查后端服务器是否运行（端口 3000）
3. 检查防火墙设置
4. 使用电脑的局域网 IP 地址访问（不要用 localhost）

### 问题：提示"密码错误"

**解决方案**：
1. 确认输入的密码正确
2. 如果忘记密码，删除 `server/data/password.json` 重新设置

### 问题：模板没有保存

**解决方案**：
1. 检查后端服务器是否正常运行
2. 查看浏览器控制台是否有错误信息
3. 检查 `server/data/` 目录是否有写入权限

## 文件说明

```
PPTist/
├── server/                          # 后端服务器
│   ├── index.js                    # Express 服务器主文件
│   ├── package.json                # 后端依赖配置
│   └── data/                       # 数据存储目录
│       ├── default-template.json   # 模板数据
│       └── password.json           # 密码哈希
├── src/
│   ├── api/
│   │   └── template.ts             # API 调用封装
│   ├── store/
│   │   ├── main.ts                 # 主 Store（密码对话框状态）
│   │   └── slides.ts               # 幻灯片 Store（模板保存/加载）
│   └── views/Editor/
│       ├── index.vue               # 编辑器主组件（密码对话框）
│       └── EditorHeader/
│           └── index.vue           # 头部菜单（触发操作）
├── TEMPLATE_SERVER_README.md       # 本文件
└── DEPLOYMENT.md                   # 部署手册
```

## 更新日志

### v1.0.0 (2025-12-28)
- ✅ 实现服务器端模板存储
- ✅ 添加密码保护机制
- ✅ 支持跨设备访问
- ✅ 自动适配网络地址
- ✅ 添加修改密码功能

## 技术支持

如有问题，请检查：
1. 后端服务器日志
2. 浏览器控制台错误信息
3. `server/data/` 目录权限

---

**提示**：详细的部署说明请参考 [DEPLOYMENT.md](./DEPLOYMENT.md)
